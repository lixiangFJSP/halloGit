package shedule;

import basedata.Data;
import enums.ScheduleType;

public class Schedule {
    /*
    要分为静态调度、插单、撤单、机器故障、返工返修，这几种情况
     */
    //获取基本数据

    //初始化调度结果（包括排程结果）

    //正常排程
//    if(Data.getType().equals(ScheduleType.SCHEDULE)) {

//    }
    //插单
//    elif(Data.getType().equals(ScheduleType.INSERT)){

//    }
    //撤单
//    elif(Data.getType().equals(ScheduleType.Revoke)){

//    }
    //机器故障

    //返工返修


}
